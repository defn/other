# Dockerfile.registry - OCI registry server image.
# Image: defn.dev/devcontainer/registry

# FROM: index.docker.io/library/registry:2
# OCI digest pinned in m/oci/images.edn, verified by: mise run dev-pull
FROM defn.dev/external/registry:2
