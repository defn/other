# Dockerfile.bazel-remote - Bazel remote cache server image.
# Image: defn.dev/devcontainer/bazel-remote

# FROM: quay.io/bazel-remote/bazel-remote:latest
# OCI digest pinned in m/oci/images.edn, verified by: mise run dev-pull
FROM defn.dev/external/bazel-remote:latest
