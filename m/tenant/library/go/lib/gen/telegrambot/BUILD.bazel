"""Go library 'telegrambot' -- generated by defn gen."""

load("@io_bazel_rules_go//go:def.bzl", "go_library")
load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file", "tagged_package")

go_library(
    name = "telegrambot",
    srcs = ["telegrambot.go"],
    importpath = "github.com/defn/other/m/tenant/library/go/lib/gen/telegrambot",
    visibility = ["//visibility:public"],
    deps = [
        "//tenant/library/go/lib/gen",
        "@org_cuelang_go//cue",
    ],
)

exports_files(["contract.cue"])

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

fmt_test(
    name = "telegrambot_go_fmt",
    src = "telegrambot.go",
    tool = "gofmt",
)

fmt_test(
    name = "deps_cue_fmt",
    src = "deps.cue",
    tool = "cue",
)

fmt_test(
    name = "contract_cue_fmt",
    src = "contract.cue",
    tool = "cue",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

tagged_file(
    name = "telegrambot_go_tag",
    src = "telegrambot.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "deps_cue_tag",
    src = "deps.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_file(
    name = "contract_cue_tag",
    src = "contract.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_package(exclude = ["telegrambot.go", "BUILD.bazel", "deps.cue", "contract.cue"])
