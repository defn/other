"""Go library 'agentedgev1connect' -- generated by defn gen."""

load("@io_bazel_rules_go//go:def.bzl", "go_library")
load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file", "tagged_package")

go_library(
    name = "agentedgev1connect",
    srcs = ["agentedge.connect.go"],
    importpath = "github.com/defn/other/m/v/buildkite--agent/api/proto/gen/agentedgev1connect",
    visibility = ["//visibility:public"],
    deps = [
        "//v/buildkite--agent/api/proto/gen",
        "@com_connectrpc_connect//:connect",
    ],
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

fmt_test(
    name = "agentedge_connect_go_fmt",
    src = "agentedge.connect.go",
    tool = "gofmt",
)

fmt_test(
    name = "deps_cue_fmt",
    src = "deps.cue",
    tool = "cue",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

tagged_file(
    name = "agentedge_connect_go_tag",
    src = "agentedge.connect.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "deps_cue_tag",
    src = "deps.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_package(exclude = ["agentedge.connect.go", "BUILD.bazel", "deps.cue"])
