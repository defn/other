"""Go library 'file' -- generated by defn gen."""

load("@io_bazel_rules_go//go:def.bzl", "go_library")
load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file", "tagged_package")

go_library(
    name = "file",
    srcs = [
        "is_opened.go",
        "opened_by.go",
    ],
    importpath = "github.com/defn/other/m/v/buildkite--agent/internal/file",
    visibility = ["//visibility:public"],
    deps = [
        "//v/buildkite--agent/internal/shell",
    ],
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

fmt_test(
    name = "is_opened_go_fmt",
    src = "is_opened.go",
    tool = "gofmt",
)

fmt_test(
    name = "opened_by_go_fmt",
    src = "opened_by.go",
    tool = "gofmt",
)

fmt_test(
    name = "deps_cue_fmt",
    src = "deps.cue",
    tool = "cue",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

tagged_file(
    name = "is_opened_go_tag",
    src = "is_opened.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "opened_by_go_tag",
    src = "opened_by.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "deps_cue_tag",
    src = "deps.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_package(exclude = ["is_opened.go", "opened_by.go", "BUILD.bazel", "deps.cue"])
