"""Go library 'tempfile' -- generated by defn gen."""

load("@io_bazel_rules_go//go:def.bzl", "go_library", "go_test")
load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file", "tagged_package")

go_library(
    name = "tempfile",
    srcs = [
        "doc.go",
        "tempfile.go",
    ],
    importpath = "github.com/defn/other/m/v/buildkite--agent/internal/tempfile",
    visibility = ["//visibility:public"],
)

go_test(
    name = "tempfile_test",
    srcs = ["tempfile_test.go"],
    embed = [":tempfile"],
    deps = [
        "@tools_gotest_v3//assert:assert",
    ],
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

fmt_test(
    name = "doc_go_fmt",
    src = "doc.go",
    tool = "gofmt",
)

fmt_test(
    name = "tempfile_go_fmt",
    src = "tempfile.go",
    tool = "gofmt",
)

fmt_test(
    name = "tempfile_test_go_fmt",
    src = "tempfile_test.go",
    tool = "gofmt",
)

fmt_test(
    name = "deps_cue_fmt",
    src = "deps.cue",
    tool = "cue",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

tagged_file(
    name = "doc_go_tag",
    src = "doc.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "tempfile_go_tag",
    src = "tempfile.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "tempfile_test_go_tag",
    src = "tempfile_test.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "deps_cue_tag",
    src = "deps.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_package(exclude = ["doc.go", "tempfile.go", "tempfile_test.go", "BUILD.bazel", "deps.cue"])
