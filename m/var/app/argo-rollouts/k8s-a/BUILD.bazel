"""Versioned app 'argo-rollouts' for k8s-a -- generated by defn gen."""

load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file")
load("//kernel/interface/app:app.bzl", "app_kustomize_versioned")

app_kustomize_versioned(
    name = "app",
    chart_tgz = "argo-rollouts-2.40.9.tgz",
    k8s_version = "1.36.1",
    parent_source_path = "tenant/library/app/argo-rollouts",
    parent_workspace_path = "var/app/argo-rollouts",
    workspace_path = "var/app/argo-rollouts/k8s-a",
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

[fmt_test(
    name = "dispatch_cue_fmt",
    src = src,
    tool = "cue",
) for src in glob(["dispatch.cue"], allow_empty = True)]

[tagged_file(
    name = "dispatch_cue_tag",
    src = src,
    tags = [
        "cue",
        "source",
    ],
) for src in glob(["dispatch.cue"], allow_empty = True)]
