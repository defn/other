"""Versioned app 'cloudnative-pg' for k8s-c -- generated by defn gen."""

load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file")
load("//kernel/interface/app:app.bzl", "app_kustomize_versioned")

app_kustomize_versioned(
    name = "app",
    chart_tgz = "cloudnative-pg-0.28.2.tgz",
    k8s_version = "1.34.8",
    parent_source_path = "tenant/library/app/cloudnative-pg",
    parent_workspace_path = "var/app/cloudnative-pg",
    workspace_path = "var/app/cloudnative-pg/k8s-c",
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

[fmt_test(
    name = "dispatch_cue_fmt",
    src = src,
    tool = "cue",
) for src in glob(["dispatch.cue"], allow_empty = True)]

[tagged_file(
    name = "dispatch_cue_tag",
    src = src,
    tags = [
        "cue",
        "source",
    ],
) for src in glob(["dispatch.cue"], allow_empty = True)]
