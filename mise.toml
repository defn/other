# Generated by defn gen from schema/versions.cue.
# DO NOT EDIT -- edit schema/versions.cue and run `mise run gen`.

[settings]
experimental = true

[env]
# Add m/bin to PATH so bbs shebang is available for mise tasks and scripts
_.path = ["bin"]

[task_config]
# includes replaces default .mise/tasks discovery; list discovered from filesystem
includes = [
  ".mise/tasks",
  "gen/.mise/tasks",
  "kernel/fmt/.mise/tasks",
  "tenant/library/infra/.mise/tasks",
]
