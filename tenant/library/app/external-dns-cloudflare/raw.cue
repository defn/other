@experiment(aliasv2,explicitopen,shortcircuit,try)

package app

// All objects are defined in app.cue (cluster-scoped with CUE tags).
// DNSEndpoint CRD is provided by the shared external-dns-crds app.
objects: {}
