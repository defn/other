"""Go library 'cache' -- generated by defn gen."""

load("@io_bazel_rules_go//go:def.bzl", "go_library", "go_test")
load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file", "tagged_package")

go_library(
    name = "cache",
    srcs = ["cache.go"],
    importpath = "github.com/defn/other/m/v/buildkite--agent/internal/cache",
    visibility = ["//visibility:public"],
    deps = [
        "//v/buildkite--agent/logger",
        "//v/buildkite--agent/version",
        "@com_github_buildkite_zstash//:zstash",
        "@com_github_buildkite_zstash//api",
        "@com_github_buildkite_zstash//cache",
        "@com_github_dustin_go_humanize//:go-humanize",
        "@in_gopkg_yaml_v3//:yaml_v3",
    ],
)

go_test(
    name = "cache_test",
    srcs = ["cache_test.go"],
    embed = [":cache"],
    deps = [
        "@com_github_stretchr_testify//require:require",
    ],
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

fmt_test(
    name = "cache_go_fmt",
    src = "cache.go",
    tool = "gofmt",
)

fmt_test(
    name = "cache_test_go_fmt",
    src = "cache_test.go",
    tool = "gofmt",
)

fmt_test(
    name = "deps_cue_fmt",
    src = "deps.cue",
    tool = "cue",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

tagged_file(
    name = "cache_go_tag",
    src = "cache.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "cache_test_go_tag",
    src = "cache_test.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "deps_cue_tag",
    src = "deps.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_package(exclude = ["cache.go", "cache_test.go", "BUILD.bazel", "deps.cue"])
