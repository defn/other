"""Go library 'trie' -- generated by defn gen."""

load("@io_bazel_rules_go//go:def.bzl", "go_library", "go_test")
load("//kernel:fmt.bzl", "fmt_test")
load("//kernel:tagged.bzl", "tagged_file", "tagged_package")

go_library(
    name = "trie",
    srcs = ["trie.go"],
    importpath = "github.com/defn/other/m/v/buildkite--agent/internal/trie",
    visibility = ["//visibility:public"],
)

go_test(
    name = "trie_test",
    srcs = ["trie_test.go"],
    embed = [":trie"],
    deps = [
        "@tools_gotest_v3//assert/cmp:cmp",
        "@tools_gotest_v3//assert:assert",
    ],
)

fmt_test(
    name = "build_bazel_fmt",
    src = "BUILD.bazel",
    tool = "buildifier",
)

fmt_test(
    name = "trie_go_fmt",
    src = "trie.go",
    tool = "gofmt",
)

fmt_test(
    name = "trie_test_go_fmt",
    src = "trie_test.go",
    tool = "gofmt",
)

fmt_test(
    name = "deps_cue_fmt",
    src = "deps.cue",
    tool = "cue",
)

tagged_file(
    name = "build_bazel_tag",
    src = "BUILD.bazel",
    tags = [
        "bazel",
        "bazel-build",
    ],
)

tagged_file(
    name = "trie_go_tag",
    src = "trie.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "trie_test_go_tag",
    src = "trie_test.go",
    tags = [
        "go",
        "source",
    ],
)

tagged_file(
    name = "deps_cue_tag",
    src = "deps.cue",
    tags = [
        "cue",
        "source",
    ],
)

tagged_package(exclude = ["trie.go", "trie_test.go", "BUILD.bazel", "deps.cue"])
